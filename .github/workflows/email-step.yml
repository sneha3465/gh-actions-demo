name: Test Email Step

on:
  workflow_dispatch:  # Allows you to trigger it manually

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1000198@ds.study.iitm.ac.in
        run: echo "Hello, world from GitHub Actions!"
